# rnd-project-memory — Publications Register

**Updated:** 2026-08-23

> `SOURCES.md` in reverse: where knowledge went, not where it came from. **Empty, and
> legitimately so** — this project publishes nothing externally; `RND_PROJECT_MEMORY.md` and the
> repository itself are the only public artefacts, and they are the handbook and the skeleton,
> not a "publication" this register is meant to track. Populated the day that changes.
>
> `stale` is a legitimate, permanent-if-needed status here, not a defect — the one register
> exempt from delete-on-resolve, because a page already read outside the project cannot be
> un-published.

---

## P-<slug> · <title>

**Where:** <external location the published copy lives>
**Version:** <published version identifier, and the date it went out>
**Collected:** <script path, and whether it is idempotent — or "by hand">
**Basis:** <what in `docs/` this rests on, plus that claim's own basis>
**Status:** current | stale — <if stale, the specific gap>
