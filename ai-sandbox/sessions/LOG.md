# rnd-project-memory — Session Log

Append only. One row per session.

**Do not read this file in full — search it.** That is what it is for:

```bash
rg -i "<topic>" ai-sandbox/sessions/LOG.md
rg '#<tag>'     ai-sandbox/sessions/LOG.md
```

A hit gives you a session file; open that, not this log.

**Outcome names what was resolved**, not just what was worked on. Answered questions are
deleted from `OPEN_QUESTIONS.md`, so this row is the only cue a later reader gets that the
question ever had an answer.

**Tags:** reuse an existing tag if one fits — a topic spelled three ways over six months is
what makes search fail. `check.sh` prints tag frequencies; a singleton usually means a second
spelling of something already here.

| Date | Topic | Tags | Outcome | Link |
|------|-------|------|---------|------|
| 2026-08-17 | Extracting the template into its own repository | `#template` `#versioning` `#distribution` `#licensing` | Six ADRs accepted: separate repo, ownership layers, vendored distribution, prose migrations, public licensed upstream, self-hosting. Found the core carrying the author's work stack; found the missing licence. | [2026-08-17-template-extraction.md](2026-08-17-template-extraction.md) |
| 2026-08-17 | Auditing file ownership across the skeleton | `#template` `#ownership` `#manifest` | ADR-002 rewritten to four layers with fenced preambles; `A-profile-separable` settled by inverting extraction into concentration, replaced by `A-profile-indirection`; `MANIFEST` drafted. Found two skeleton defects: the README overwrites the adopting project's, and two `check.sh` checks misfire. | [2026-08-17-ownership-audit-2.md](2026-08-17-ownership-audit-2.md) |
| 2026-08-17 | Testing the profile remedies, and what they cost | `#template` `#ownership` `#versioning` | Three experiments. Fencing withdrawn and the preamble files reclassified as scaffold; the secret-scan list must not be profile-supplied; `A-profile-indirection` confirmed and retired, `A-misdirection-criterion` raised. Step 4 kept but reduced; ADR-004's rule-change gap closed; `Q-upstream-identity` and `Q-handbook-version` answered. Scope fell from twenty files to six. | [2026-08-17-profile-experiments-3.md](2026-08-17-profile-experiments-3.md) |
| 2026-08-17 | Step 7, and the first real upgrade | `#versioning` `#upgrade` | `MIGRATIONS.md` and `upgrade-template.md` written, so §15 describes what exists rather than what was promised. `v1.1.0` released, its notes naming the `RULES.md` change as ADR-004 requires. The root memory upgraded from `v1.0.0` — and step 3 caught that it had never actually been at `v1.0.0`: the tag was cut mid-session and two fixes landed after it. Delete-and-copy across 15 mechanism entries produced no conflicts. | [2026-08-17-first-self-upgrade-4.md](2026-08-17-first-self-upgrade-4.md) |
| 2026-08-17 | The manifest-hash check, and applying yesterday's lesson | `#versioning` `#upgrade` | `check.sh` gains the check ADR-003 promised; `.template-hashes` ships with each release so a consumer verifies offline. `v1.2.0` released and the root upgraded to it. The first cut was noisy and the tag was re-cut before publication — the v1.0.0 tagging lesson applied on its first opportunity. Tested against a real edit, not only a clean tree. | [2026-08-17-manifest-hash-check-5.md](2026-08-17-manifest-hash-check-5.md) |
| 2026-08-19 | Enterprise access confirmed | `#distribution` | Cloning from inside the work network verified. `Q-enterprise-access` retired: access answered, forking moot under ADR-005, intake carried forward as `Q-oss-intake` 🟢. Removes the manual carry-in path and the first of ADR-005's three conditions for building an enterprise copy. | [2026-08-19-enterprise-access-confirmed.md](2026-08-19-enterprise-access-confirmed.md) |
| 2026-08-23 | Skeleton v2.0.0 — thread checkpoints and negative knowledge | `#template` `#ownership` `#versioning` `#upgrade` | All 30 techniques from the design review implemented as one MAJOR release. ADR-007 accepted: checkpoint axis is now thread, not owner; negative knowledge (distrust, scope limits, legitimate absence) named as a first-class category with fields. `MIGRATIONS.md` carries its first real structural section. Root memory raised to `v2.0.0`; `CHECKPOINT-esdevop.md` closed, not renamed — nothing was left in progress once its one live item was fixed. | [2026-08-23-skeleton-v2-thread-checkpoints.md](2026-08-23-skeleton-v2-thread-checkpoints.md) |
| 2026-08-24 | v2.1.0 — closing gaps found in v2.0.0's own rollout | `#template` `#versioning` `#upgrade` | Three stray `CHECKPOINT-<owner>.md` references fixed; `check.sh` gains "Retired vocabulary" and "skeleton/ vs `.template-version`" checks so both classes of miss are caught mechanically going forward. `MIGRATIONS.md`'s release procedure updated to feed future retired terms into the check. `v1.0.0`–`v1.2.0` found already tagged on the artefact commit; `v2.0.0` and `v2.1.0` tagged the same way, retroactively for the former. Root memory raised to `v2.1.0`. | [2026-08-24-v2-1-0-drift-tags.md](2026-08-24-v2-1-0-drift-tags.md) |
| 2026-08-25 | Findings from an external adoption trial, and v2.2.0 | `#template` `#versioning` `#upgrade` `#adoption` | First evidence from an adopter outside this repository. `ADR-008`–`ADR-011` accepted, all four signed off by a named human — the first use of that configuration here. The hash list may now list only files installed verbatim; three entries broke that and reported FAILED on every correct adoption, none visible to self-hosting, which performs no installation. `.gitignore` gains a two-region owner split; the adoption note is sanctioned with a fixed form and a boundary; the bump discriminator is bounded by inert-or-expiring. §11 rewritten to eleven steps and now says bootstrapping *begins* there. `bootstrap-test.sh` added as a release gate. Root memory raised to `v2.2.0`, which exercised the new `.gitignore` branch and found its fallback assumes the wrong cause (`Q-marker-absence-reasons`). | [2026-08-25-adoption-trial-intake.md](2026-08-25-adoption-trial-intake.md) |
| 2026-08-25 | Anchoring two checks, and the rule they made explicit | `#template` `#versioning` `#upgrade` | `v2.3.0`. **`Q-check-reads-prose-as-state` answered and deleted** — the open-session check now anchors on the field form, so a closed record that quotes the string while describing the check is no longer reported as interrupted. **`Q-marker-absence-reasons` answered and deleted** — the `.gitignore` marker check distinguishes "predates the layout" from "never took the never-commit list" instead of asserting the first. New rule: a record that describes a check is never rewritten to satisfy it — nothing enforces it mechanically and that is recorded, not papered over. Bump settled as MINOR on the consumer's obligation: PATCH says "nothing to do", and this ships a behavioural rule. Root raised to `v2.3.0`. | [2026-08-25-check-anchors-and-the-reader-rule-2.md](2026-08-25-check-anchors-and-the-reader-rule-2.md) |
| 2026-08-25 | The README's Status section, two releases stale | `#template` `#versioning` | `README.md` claimed `v2.1.0` after `v2.2.0` and `v2.3.0` had shipped. Rewritten to lead with the external adoption — the first independent evidence this project has — and to state the general form of the dogfooding lesson for a reader who will never run the template. Cause closed, not just the symptom: "Cutting a release" never named `README.md`, so the one file a reader meets before adopting was the one the procedure forgot. Step 3b added. `MIGRATIONS.md` is norcopy, so no release. | [2026-08-25-readme-status-3.md](2026-08-25-readme-status-3.md) |
| 2026-08-26 | Clone-local settings, and the contributor they never reach | `#template` `#versioning` `#ownership` | `v2.4.0`. Opened on the owner token with two contributors; the answer was already in the rules (`Held by:` is whoever holds the thread), and the real defect was that `git clone` does not copy `.git/config` — so `core.hooksPath` is unset in every clone but the adopter's, the secret scan sits inert in the working tree with no warning from git, and `skeleton/README.md` is `norcopy` so the instruction never arrives either. Demonstrated in a scratch repository. `AGENTS.md` gains "First run in a new clone", `check.sh` a first section reporting both settings by value, `session-start.md` step 0a (found by release step 2b — the playbook governing session start did not know the rule). Nothing added to `pre-commit`: it does not reach a second contributor and a false positive there disables the secret scan sharing that hook. **`Q-held-by-identity-binding` raised** — bind `Held by:` to `git config user.email`, evaluated in full, deferred as MAJOR rather than holding the security half hostage to it. Root raised to `v2.4.0`. | [2026-08-26-clone-local-settings.md](2026-08-26-clone-local-settings.md) |
| 2026-08-26 | The glossary's owner-token entry, four releases stale | `#template` `#ownership` | `docs/glossary.md` still defined *owner token* as "the initials naming one person's checkpoint file" — the pre-`v2.0.0` meaning, retired when the checkpoint axis moved from the person to the thread. Misdirecting in `ADR-002`'s sense: it instructs, and the instruction is false. Rewritten to lead with who holds a thread, carry the "not a filename" negative explicitly, and cite `ADR-007`. Why `v2.1.0`'s retired-vocabulary check missed it is the keeper: that check matches literal strings from a migration's Reason section, and this entry expressed the retired *idea* in words nobody had listed — which is what a definition does. Covers restatements of a retired term, not of a retired meaning. Not mechanised: matching paraphrase means guessing phrasings in advance, and the class has produced one instance. `docs/glossary.md` is `scaffold` and the skeleton's copy is an empty table, so no release. | [2026-08-26-glossary-owner-token.md](2026-08-26-glossary-owner-token.md) |
| 2026-08-26 | v3.0.0 — the holder is the clone's git identity | `#template` `#versioning` `#upgrade` `#ownership` | **`Q-held-by-identity-binding` answered and deleted.** `ADR-012` accepted and signed off, extending `ADR-007`: `Held by:` is the exact output of `git config user.email` in the clone where the work happens, and the declared per-person token is retired. A per-person value in a per-project file has one slot and N contributors, and the obvious repair merges *cleanly* the wrong way — last writer wins, no conflict marker. New rule in `RULES.md`: the value is never inferred, and empty output is a stop. `Owner:` on register entries splits off as a human name — one token becomes two conventions, priced in the ADR rather than found later. `check.sh` gains a per-checkpoint `Held by:` comparison (placeholders skipped, or every clean adoption opens with a mismatch) and `RETIRED` gains `owner token`, `<owner>`, which immediately flagged two prose files written this week describing the retirement in the retired words. Enforcement deliberately advisory and not in `pre-commit`, which no second contributor has. Residual risk recorded as `A-identity-is-the-person`. Root raised to `v3.0.0`. | [2026-08-26-v3-0-0-held-by-binding-3.md](2026-08-26-v3-0-0-held-by-binding-3.md) |
| 2026-08-26 | Do the instructing documents say what the system now does? | `#template` `#versioning` `#upgrade` | `v3.0.1`, PATCH. Audit of every instructing document against what `v2.4.0` and `v3.0.0` implemented. One real contradiction: `upgrade-template.md` step 4 forbade touching `scaffold` and `content` while step 6 requires exactly that for the `v3.0.0` migration — first migration to need scaffold edits, so it was latent until now; step 4 now scopes itself to the wholesale replacement. `skeleton/README.md` step 2 still described hook installation as a one-time adoption step, the precise misconception `v2.4.0` exists to correct, sitting in the install guide; the two clone-local settings now have one home there instead of two. The handbook was behind the skeleton on both releases in §11, §12 and §15 — which matters because `skeleton/README.md` says the handbook wins where they disagree. **`check.sh` reported `clean` throughout**: the retired-vocabulary check matches retired strings and is blind to a sentence that was true under the old design and false under the new one — second instance today, after `docs/glossary.md`. Not mechanised; noted for the next release. | [2026-08-26-doc-alignment-audit-4.md](2026-08-26-doc-alignment-audit-4.md) |
| 2026-08-26 | Who keeps the history, and the placeholder class that raised the question | `#template` `#adoption` | **`Q-who-keeps-the-history` raised** 🟡 — ownership was already settled (`ADR-012`; the assistant is nowhere a subject of record), execution is not, and a credit ceiling makes that a silent availability dependency. The concrete cause: `skeleton/README.md` step 3 said *replace every `<PROJECT_NAME>` and `<PLACEHOLDER>`*, and no such token exists — it appeared once, in that sentence. One syntax carried three classes with three lifetimes (20 mechanical tokens, 7 human blanks, ~170 example-syntax occurrences), so the instruction had no consistent reading and an install following it destroys `INDEX.md`'s routing table. Human blanks now marked `<<FILL: …>>`; `check.sh` counts them and names the two session-loaded files differently; `bootstrap-test.sh` gains three gates, one asserting the example syntax *survives*. Two defects found in the change itself: a check that names what it looks for rewrote its own installed copy and stripped its executable bit, and a mechanism file may not spell out a token the install substitutes — `ADR-008`'s criterion catching a fresh instance on the first gate. §12's `check.sh` row found three releases stale and corrected. Three install defects remain untouched; thread `install-path` opened. Also: `checkpoint.md` told the assistant to number the day's sessions in order of arrival, which §4 forbids (sessions carry no counter) and `docs/constraints.md` binds against (no shared counter) — this session's own filename carried a `-5` before a human read it. Playbook and §4 both corrected; existing filenames keep their suffixes, being cited from here. | [2026-08-26-who-keeps-the-history.md](2026-08-26-who-keeps-the-history.md) |
| 2026-08-26 | Enforcing the session-filename rule that had just been written | `#template` `#adoption` | `check.sh` gains a session-counter check, deliberately in its own commit so that the interval during which the corrected rule was prose-only is visible in history rather than folded away. The obvious invariant — a suffixed file has a same-slug sibling — is permanently violated by eight existing records, since records are never renamed, and shipping it would have put a standing complaint in every run (`ADR-008`'s cost). Aimed instead at **session files not yet committed**: it catches the mistake while fixing it is still free, says nothing about history, and works on a file written by hand — which is the property a generator does not have and `Q-who-keeps-the-history` turns on. Precedent already in `check.sh`, whose `docs/`/`CLAIMS.md` check reads staged changes. Verified on four cases, one of them yesterday's `-5`. Found: closing a session and saving are the same gesture with different consequences, and nothing tells them apart at the moment of use — recorded, not acted on. | [2026-08-26-session-counter-check.md](2026-08-26-session-counter-check.md) |
| 2026-08-26 | Testing the prose-and-script claim instead of asserting it | `#template` `#adoption` | `EXP-2026-08-26-prose-script-restatement`, *supports, with one claim withdrawn*. **The `RETIRED` claim is withdrawn**: the array is in sync with every retired-terminology list, and the two failures cited when the claim was made were the check matching strings while the idea had been paraphrased — a coverage limit, not drift. The positive: `bootstrap-test.sh` never performs README step 4, so a by-the-book install leaves `CHECKPOINT-thread.md` unrenamed, and the adopter's `check.sh` prints `ok` on it — not a missing check but two correct checks whose exemptions overlap. Deliberately searched for an unforced divergence rather than injecting one. `install.sh` stays a proposal; that extraction collapses the duplication is itself untested. **`Q-session-boundary` raised** after the third awkward session boundary in one day. | [2026-08-26-testing-the-restatement-claim.md](2026-08-26-testing-the-restatement-claim.md) |
| 2026-08-26 | Extracting the installer, and measuring what it changed | `#template` `#adoption` | `install.sh` extracted; `bootstrap-test.sh` calls it instead of reproducing it; `skeleton/README.md` points and describes no mechanical step in prose. **Gap 4 closed** — the install performs the checkpoint rename when given a slug, refuses it without a clone identity rather than guessing a holder, and the gate asserts it. **`EXP-2026-08-26-install-extraction-cost` contradicts**: Gap 4 touched three files against a pre-registered prediction of one and a kill criterion of two. The metric was wrong and visibly so in advance — counting files conflates duplicated facts with separated concerns. A post-hoc count showing copies falling from two places to one is recorded as weaker and its question pre-registered rather than claimed. `Q-who-keeps-the-history` explicitly **not** advanced. Found: the self-naming `<<FILL>>` marker caught a third tool, each rediscovery by running rather than reading; removing README's numbered steps broke nine cross-references. | [2026-08-26-extracting-the-installer.md](2026-08-26-extracting-the-installer.md) |
| 2026-08-26 | Raising the root memory to v3.1.0 | `#template` `#versioning` `#upgrade` | `v3.1.0` released, MINOR, both exceptions checked inert against `ADR-011`. Root raised. **Three defects, all found by running the procedure rather than reading it.** The unfilled-blank check matched prose about the marker, so it reported nine files in the repository that ships it — correct for every adopter, wrong only for the author, which is this project's blind spot reversed and worse for it: the person who can fix it is the one trained to ignore it. Anchored to the line start like the `.gitignore` check. `upgrade-template.md` step 4 never said to copy `.template-hashes` (not in `MANIFEST` — a file cannot carry its own hash), done by hand at every upgrade and written at none; step 5 diffed `RULES.md` alone and would have reported "no rule changes" for a release delivering two through a playbook. Tag re-cut before publication, `v1.2.0`'s precedent. `bootstrap-test.sh` passed throughout and could not have caught the first: a scratch install has no prose. | [2026-08-26-raising-the-root-to-v3-1-0.md](2026-08-26-raising-the-root-to-v3-1-0.md) |
| 2026-08-26 | Do the instructing documents say what v3.1.0 does? | `#template` `#versioning` `#upgrade` | `v3.1.1`, PATCH — the alignment pass `v3.0.1` was, for the same reason. Fourteen findings: the sweep found eight (§11 steps 1 and 11, §15 twice, `MIGRATIONS.md` 2a, `check.sh`, `MANIFEST`, the root `README.md` never mentioning the installer), the reading found the six that mattered. **Sharpest: `MIGRATIONS.md` step 4 names changed rules only if `RULES.md` changed — the same blind spot as `upgrade-template.md` step 5, fixed hours earlier on the receiving end without looking at the sending end.** Fixing one half made the other *less* likely to be noticed, not more. Three defects were in the install guide, hours old, mine: a duplicated line, a run-on paragraph, and `[first-thread-slug]` offered in square brackets in the guide about which brackets are literal. §15 was corrected against the playbook rather than the reverse — the handbook-wins rule governs the skeleton, not a repair the skeleton reached first. `check.sh` `clean` throughout. | [2026-08-26-alignment-audit-v3-1-0.md](2026-08-26-alignment-audit-v3-1-0.md) |
| 2026-08-26 | The clone that cannot say which release it is | `#template` `#versioning` `#adoption` | Rehearsing the adoption path from a **clone** rather than the working tree — the step nothing had exercised — found `install.sh` writing `v0.0.0-untagged` into `.template-version` and reporting it `ok`. A `--depth 1` clone carries no tags, and the path-limited log returns the wrong commit; both silently. Field one of that file is what `check.sh` reports and what §15 measures every upgrade from, so such a project can never be upgraded correctly and nothing looks wrong. Both conditions now refuse **before the copy set is touched** (destination left at 0 files, verified), applying the rule already written for `user.email` forty lines away: a fact that cannot be derived is a stop, not a fallback. `install.sh` is `norcopy`, so no release. `bootstrap-test.sh` could not have caught it and still cannot — it installs from a working tree that is never shallow. Local staging closed as an experiment setting: it earned its keep as a rehearsal, but the button, the rendered landing page and access do not exist locally, and a stage needs an instruction the documentation does not contain. | [2026-08-26-the-shallow-clone.md](2026-08-26-the-shallow-clone.md) |
| 2026-08-26 | Removing the hazard changes what the warning should say | `#template` `#distribution` | *Template repository* unticked and the button confirmed gone. The `v3.1.0` paragraph warning against it now described a control that is not there, and a reader finding no button concludes the docs are stale — so it was re-aimed, not deleted. Kept because **the switch is a repository setting**: not in this repository, unreadable by any check here, and silently reversible. Same family as `core.hooksPath` (`v2.4.0`) but one step further out — that one is at least reportable locally; this is the first surface the project depends on that nothing here can watch, and the paragraph is the whole mitigation. The rendered-README question was badly posed: the remote is `v3.0.1`, `install.sh` first appears in `v3.1.0`, so no README there can mention it — that check belongs after the push. `norcopy`, so no release. | [2026-08-26-the-button-that-is-gone.md](2026-08-26-the-button-that-is-gone.md) |
| 2026-08-26 | The release procedure that never said to publish | `#template` `#versioning` `#distribution` | `git push --tags` pushed the tags without the branch, leaving the remote with a version that exists and content that does not — the tag resolves, the landing page is the previous release, and the push succeeds silently. Not an error against the instruction: **"Cutting a release" ended at step 5 and said nothing about publishing** — not what, not in what order, not with which command. Same class as the gap that left `README.md`'s Status two releases stale, and found the same way: by someone doing it. Step 6 added, `git push --follow-tags`, with why `--tags` alone produces the split. `MIGRATIONS.md` is `norcopy`, so no release. Also corrected: an earlier claim that the remote carried no `v3.1.0` tag rested on empty `git ls-remote` output, which an auth failure and a real absence produce identically — the conclusion held only because `git log origin/main..HEAD` supported it independently. | [2026-08-26-the-button-that-is-gone.md](2026-08-26-the-button-that-is-gone.md) |
| 2026-08-26 | What the green-start experiments returned | `#template` `#adoption` | Two records. **Assisted: supports, with one field failing.** The three sections the paragraph underdetermined were written as explicit absences with pointers into three new `OPEN_QUESTIONS.md` entries — no invented facts, no operator input, zero questions asked, `check.sh` clean. Then every entry was stamped `Owner: esdevop@gmail.com`, thirteen lines under a bold rule in the same file forbidding exactly that. The field carries two rules and no third exit — always filled, not an address — and nothing says *ask*: prose gave it an escape and it used it, a structured field gave it none. Two markers were answered leaving their closing bracket, in the two session-loaded files, and `check.sh` said `ok` — **this morning's anchoring traded a false positive for a false negative and it came due the same day.** **Manual: supports. 12:11**, the number `Q-who-keeps-the-history` had been carrying an assertion in place of; a lower bound, endpoint *installed* not *ready*. Its four costs are all documentation defects: the guide's example makes the destination path and the project name read as one string, so the person supplied the folder name and got `# churn-signals` where the assistant got `# Churn Signals` — **the assistant beat the person at the parameter whose documentation is ambiguous.** Neither arm produced a `README.md`; only the person noticed. Nothing fixed here, deliberately. | [2026-08-26-green-start-results.md](2026-08-26-green-start-results.md) |
| 2026-08-27 | The independent arm, and what it did to yesterday's result | `#template` `#adoption` | **`EXP-2026-08-27-green-start-independent` contradicts**, on the arm that outranks yesterday's: different provider, `CONFIGURATIONS.md`'s *meaningfully better* rung. The three underdetermined sections were filled confidently — three success criteria with named metrics, three stakeholder categories where one was given, four scope exclusions — and `OPEN_QUESTIONS.md` left as the shipped template, zero entries. The trace records it as a decision: *"the user didn't provide specific prompts, so I can avoid creating new questions"*. **The pair is not "one passed, one failed"** but: the instruction not to invent is not load-bearing across providers. Mechanism found — that rule lives only in `skeleton/README.md`, which is `norcopy` and never reaches a project; the markers carry what to write and never the prohibition. `v2.4.0`'s shape again. One apparent invention is ours: the skeleton ships `DATA_ENVIRONMENT.md` asserting `uv` as fact and `AGENTS.md`'s marker says the stack must match it. **`check.sh` clean on both arms** — one of honest absences, one of confident fiction; that boundary is now measured. Yesterday's record not edited: records are immutable, corrections go in the next one. | [2026-08-27-the-independent-arm.md](2026-08-27-the-independent-arm.md) |
| 2026-08-27 | Fixing the two causes, and releasing v3.2.0 | `#template` `#versioning` `#upgrade` `#adoption` | Only the two findings that could have changed the green-start result, so a re-run measures them and not a bundle. **Cause one was a layer mistake**: *fill only from what you were told* lived in `skeleton/README.md`, which is `norcopy` — an absent rule, not a weak one. "Move it to `RULES.md`" was the wrong instruction: three readers need it at three moments and `RULES.md` covers only the second, so it is authority there, pointer in the guide, short form at each marker — the last deliberately not relied on, since the success-criteria marker already carried its escape and the independent arm declined it. **Cause two was ours**: `DATA_ENVIRONMENT.md` asserted `uv` as fact while `AGENTS.md` ordered the stack to match it, so the assistant was obeying, not inventing; fixed in both files, because an example an instruction orders you to match is not an example. House style already existed one file away in `gitignore.template`. **The handbook had forbidden this for four releases — the artefact was wrong and the authority was right**, the direction the handbook-wins rule exists for and the first time it has run that way. `RATIONALE.md` gains both rules and two failure-mode rows. `v3.2.0` MINOR; root raised. Six findings left untouched on purpose. The marker caught a fourth tool — the rule about it. | [2026-08-27-fixing-the-two-causes.md](2026-08-27-fixing-the-two-causes.md) |
| 2026-08-27 | ADR-013, and the rule it immediately withdraws | `#template` `#versioning` `#adoption` | **`ADR-013` accepted and signed off** — the instrument follows detectability, not severity: a rule only where no check can see the failure, a check where late detection is a remedy, a hook plus a rule where it is not. Three gates for a new rule, two evidence asymmetries. It earns its place by flagging **our own** error: two of `v3.2.0`'s five edits fail it. **`v3.3.0` withdraws one of them** — *a field that must not be blank and has no correct value is a stop, ask* — which the next assistant read, judged would block progress, and answered by skipping the register entirely, so the open questions became prose in `docs/` where nothing sweeps them. The defect was structural: the preamble said `Owner:` is *always filled* and that blank means *nobody has claimed this*, which cannot both hold. Preamble fixed, `check.sh` gains the `@` check that should have been written first, `RATIONALE.md` keeps the withdrawn rule as a record of its cost. Marker flags untouched on purpose — they fail gate 3 but the only measured configuration includes them. Next two runs pre-registered before either is run. | [2026-08-27-adr-013-and-the-rule-it-withdraws.md](2026-08-27-adr-013-and-the-rule-it-withdraws.md) |
| 2026-08-28 | The probe passed, and cause A held | `#template` `#adoption` | **`EXP-2026-08-28-probe-and-cause-a`, supports.** Both pre-registered outcomes identical across two runs of the identical condition, so the stopping rule does not fire and **Run 2 is authorised**. Cause A held and replicated: where the `v3.2.0` run left the register untouched — its trace reasoning aloud about *always filled in* — entries now appear in both runs with `Owner:` blank, which the amended preamble sanctions. The named mechanism, in the predicted field, twice. **The coarse outcomes decided the experiment**: the identical condition gave two entries in one run and one with merged scope in the other, so a *count* metric would have failed the probe and cancelled Run 2 by the metric rather than the instrument. **The confound that materialised was the one nobody watched** — `C-copilot-model-build` was written about an invisible model build, and the CLI moved visibly v1.0.80 → v1.0.81 between baseline and runs; A is supported-with-a-confound, not clean. Caveat corrected in place: record the harness version every run. | [2026-08-28-the-probe-passed.md](2026-08-28-the-probe-passed.md) |
| 2026-08-28 | The third gate costs nothing, and the arc closes | `#template` `#adoption` | **`EXP-2026-08-28-marker-flags`, supports.** Neither pre-registered outcome moved when the seven marker restatements were removed, on the same CLI build as both prior runs — the cleanest measurement of the series. `ADR-013`'s third gate has no measured price on its first application. **The finding is as much what was refused:** entries came out 1, 2, 3 across three near-identical runs and Run 2 named four success dimensions where the others named none, so a story was available that removing the flags made it more expansive. Not readable — Run 1′ had already moved 2 → 1 under a *completely identical* condition — and recorded as refused rather than omitted. The arc closes: invention stopped at `v3.2.0` (rule into a loaded file, stack no longer asserted), register use returned at `v3.3.0` (preamble contradiction removed), `v3.4.0` changed neither. Three fixes, three confirmed mechanisms, one norm shown to cost nothing, each tested one at a time. | [2026-08-28-the-gate-costs-nothing.md](2026-08-28-the-gate-costs-nothing.md) |
| 2026-08-28 | Where the green-start evidence lives, and what was in it | `#template` `#adoption` `#secrets` | A housekeeping question — copy the artefacts in? — answered no, on a binding constraint. The raw logs carry a work email domain (three times in one log, and as `user.email` in one tree's `.git/config`) and a work account name thirty-seven times; `docs/constraints.md` binds this **public** repository against employer-identifying content *including in history*. **The secret hook would not have caught it** — its patterns match credential *values*, not identifiers, deliberately, so the constraint was met by someone looking rather than by machine, and that is recorded as a near miss rather than a success. In the repository instead: `green-start-arms__*.json` in `ai-sandbox/results/` — the summary §8 anticipates for *raw, possibly gitignored, data* — with every arm's version, model, interface, cost, both pre-registered outcomes, and the sha256 of each raw file. The reason the five records point outward is stated in `results/README.md`, not by editing immutable records. **`Q-oss-intake` raised 🟢 → 🟡**: Run 2 ran on a work machine under a work identity against this public repository, so the question describes something that happened rather than something that might. | [2026-08-28-where-the-evidence-lives.md](2026-08-28-where-the-evidence-lives.md) |
